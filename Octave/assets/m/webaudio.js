// 生成物，勿手改：由 build/assets.py bundle-m 产出
window.__OCT_ASSETS__ = window.__OCT_ASSETS__ || {};
window.__OCT_ASSETS__["webaudio"] = {
  addpath: ["/usr/src/octave/m/webaudio"],
  files: {"/usr/src/octave/m/webaudio/__pba_enqueue__.m": "## Append a playback action for the page to drain (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Octave cannot call into JS synchronously, so playback requests are queued in\n## a file that bridge/webaudio.js polls after each eval.  One line per action:\n##\n##   <id>\\t<action>\\t<arg…>      通用形状（行格式的读侧声明见 bridge/webaudio.js 的\n##                               `parseLine`，两边由 test/browser/accept-queue-drift.mjs 对齐）\n##   各动作的参数（**这里才是权威清单**，别再各写一份）：\n##     play     <from> <to> <rate> <channels>      to=0 表示\"播到结尾\"\n##     resume   <from> <to> <rate> <channels>      与 play 同形（页面侧走同一条代码路径）\n##     pause / stop 无参数\n##\n## The audio samples live next to it in /tmp/pba_<id>.f64 (raw little-endian\n## doubles, interleaved by column for stereo), written by __player_play__.\n\nfunction __pba_enqueue__ (id, action, varargin)\n\n  fid = fopen (\"/tmp/pba_queue.txt\", \"a\");\n  if (fid < 0)\n    error (\"audioplayer: cannot write /tmp/pba_queue.txt\");\n  endif\n  fprintf (fid, \"%d\\t%s\", id, action);\n  for k = 1:numel (varargin)\n    fprintf (fid, \"\\t%.17g\", varargin{k});\n  endfor\n  fprintf (fid, \"\\n\");\n  fclose (fid);\n\nendfunction\n", "/usr/src/octave/m/webaudio/__pba_get__.m": "## Read one property of a player record (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## FIELD is the Octave-facing property name (e.g. \"SampleRate\"); unknown names\n## return [] rather than erroring, because __get_properties__ asks for the full\n## set in one go and a hard failure there would break `disp (player)`.\n\nfunction v = __pba_get__ (id, field)\n\n  __pba_init__ ();\n  global __pba__;\n\n  v = [];\n  if (id < 1 || id > numel (__pba__.items)), return; endif\n  rec = __pba__.items{id};\n  if (isstruct (rec) && isfield (rec, field))\n    v = rec.(field);\n  endif\n\nendfunction\n", "/usr/src/octave/m/webaudio/__pba_id__.m": "## Pull the integer id out of an audioplayer handle (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## @audioplayer passes `struct (player).player` to every __player_* builtin, so\n## the handle is a struct with an Id field.  Accepting the bare id and the\n## classdef object too makes the .m implementations forgiving when called\n## directly (which the acceptance suite does).\n\nfunction id = __pba_id__ (handle)\n\n  id = 0;\n\n  if (isnumeric (handle) && isscalar (handle))\n    id = handle;\n    return;\n  endif\n\n  if (isstruct (handle))\n    if (isfield (handle, \"Id\"))\n      id = handle.Id;\n    elseif (isfield (handle, \"player\") && isstruct (handle.player) ...\n            && isfield (handle.player, \"Id\"))\n      id = handle.player.Id;\n    endif\n    return;\n  endif\n\n  ## a classdef audioplayer object: the payload is in its .player field\n  try\n    p = struct (handle).player;\n    if (isstruct (p) && isfield (p, \"Id\"))\n      id = p.Id;\n    endif\n  catch\n    id = 0;\n  end_try_catch\n\nendfunction\n", "/usr/src/octave/m/webaudio/__pba_init__.m": "## WebAudio-player backing store for the wasm bridge (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## The 18 __player_* builtins that @audioplayer calls are implemented here in\n## pure .m — no .oct, no PortAudio, no compile step.  Every one of them takes an\n## opaque *handle* and reads or writes properties, so the handle can be a struct\n## holding a single integer id into a global table:\n##\n##   __pba__.items{id}  the player's properties\n##   __pba__.next       next free id\n##\n## Access is via __pba_new__ / __pba_get__ / __pba_put__, because a function\n## that *returned* the global table would hand back a copy and every mutation\n## would be silently lost.\n##\n## Playback is deferred to the page: `play` appends an action to\n## /tmp/pba_queue.txt and the JS side (bridge/webaudio.js) drains it into an\n## AudioContext + AudioBufferSourceNode.  Octave cannot await JS, so the page is\n## the only party that can actually start playback — and browsers only allow\n## that after a user gesture anyway.\n\nfunction __pba_init__ ()\n\n  global __pba__;\n  if (isempty (__pba__))\n    __pba__ = struct ();\n    __pba__.items = {};\n    __pba__.next = 1;\n  endif\n\nendfunction\n", "/usr/src/octave/m/webaudio/__pba_new__.m": "## Allocate a player record (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Returns the integer id; the handle handed to Octave's @audioplayer class is\n## a struct wrapping it (see __player_audioplayer__).\n\nfunction id = __pba_new__ (props)\n\n  __pba_init__ ();\n  global __pba__;\n\n  id = __pba__.next;\n  __pba__.next = id + 1;\n  __pba__.items{id} = props;\n\nendfunction\n", "/usr/src/octave/m/webaudio/__pba_now__.m": "## Monotonic wall-clock seconds for playback bookkeeping (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Used to estimate CurrentSample and to decide when a non-looping player has\n## finished.  `time ()` has 1-second resolution in Octave; `tic`/`toc` are tied\n## to their own timer state, so a plain process-start offset plus time() is the\n## portable approximation.  CurrentSample is therefore coarse (±1 s), which is\n## the honest limit of a build with no audio clock — the page's own\n## AudioContext.currentTime is authoritative for anything that needs precision.\n\nfunction t = __pba_now__ ()\n\n  t = time ();\n\nendfunction\n", "/usr/src/octave/m/webaudio/__pba_put__.m": "## Write one property of a player record (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction __pba_put__ (id, field, value)\n\n  __pba_init__ ();\n  global __pba__;\n\n  if (id < 1 || id > numel (__pba__.items)), return; endif\n  rec = __pba__.items{id};\n  if (! isstruct (rec)), rec = struct (); endif\n  rec.(field) = value;\n  __pba__.items{id} = rec;\n\nendfunction\n", "/usr/src/octave/m/webaudio/__pba_transition__.m": "## Octave-Full-Wasm — **播放状态机**（唯一拥有者，own code, repo license）\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## ── 为什么要有它（2026-09-23 胶水层审计候选 5）───────────────────────────────\n## 播放器的状态是六个字段（`Running` / `PlayingFrom` / `PlayingTo` / `StartTime` /\n## `PausedAt` / `CurrentSample`），而它们以前由**六个 module 各自维护**：\n##   `__player_play__`（写 5 个）、`__player_pause__`（冻结时钟）、\n##   `__player_resume__`（平移 StartTime）、`__player_stop__`（清 6 个）、\n##   `__player_isplaying__`（**在谓词里**判\"播完了\"并写 2 个）、\n##   `__player_playblocking__`（自己再写一遍\"播完了\"那 2 个）。\n## ⇒ 同一件事（时长/已播时间/播完）算三遍，\"播完\"写两遍，谁都没法单独看懂这条状态机。\n##\n## 现在：**只有本文件写这六个字段**。六个 `__player_*` 保留原签名，只负责\"取参数 → 调这里\n## → 入队给页面\"。于是\"状态机对不对\"第一次可以在**没有浏览器**的情况下测（见文末 `%!test`）。\n##\n## ── 状态机 ──────────────────────────────────────────────────────────────────\n##                     play(s,e)                finish / tick(到点)\n##      (无) ─────────────► running ────────────────► off\n##                            │  ▲                      ▲\n##                     pause  │  │  resume              │ stop\n##                            ▼  │                      │\n##                         paused ─────────────────────┘\n##   · play 之后再 play、pause 之后再 pause、resume 只对 paused 有效（其余都是 no-op）\n##   · **play 必须清 PausedAt**：否则 elapsed 会用上一轮的暂停点算，得到一个负数\n##     （旧代码就有这个洞：`play; pause; play` 之后 `CurrentSample` 会算出负值）\n##\n## ── 为什么\"查询也能推进状态\"（tick 事件）─────────────────────────────────────\n## 本构建**没有定时器**（Octave 侧拿不到页面的事件循环，`pause()` 期间页面完全停摆，\n## 见 HISTORY §5.10）。所以\"播完了\"这件事只能**被查出来的**：`isplaying`/`playblocking`\n## 顺带推一次时钟（tick），到点就转 off。这不是设计上的漂亮解，是环境约束下的唯一解 ——\n## 写在这里免得下一个人把它当 bug\"修\"掉。\n##\n## ── 接口 ────────────────────────────────────────────────────────────────────\n##   [tf, dur] = __pba_transition__ (id, event, ...)\n##     event = \"play\"（后跟 1 基的 s, e）| \"pause\" | \"resume\" | \"stop\"\n##           | \"tick\"（推进时钟，返回还在不在跑）| \"finish\" | \"duration\"（只为了拿 dur）\n##     tf  = 事件之后这个播放器是否在跑\n##     dur = 排定区间时长（秒）；没排定就是 []\n\nfunction [tf, dur] = __pba_transition__ (id, event, varargin)\n\n  tf = false;\n  dur = [];\n\n  if (! (isscalar (id) && id >= 1))\n    return;\n  endif\n\n  fs = __pba_get__ (id, \"SampleRate\");\n  s = __pba_get__ (id, \"PlayingFrom\");\n  e = __pba_get__ (id, \"PlayingTo\");\n  t0 = __pba_get__ (id, \"StartTime\");\n  paused_at = __pba_get__ (id, \"PausedAt\");\n\n  scheduled = (! isempty (fs) && fs > 0 && ! isempty (s) && ! isempty (e) && ! isempty (t0));\n  if (scheduled)\n    dur = (e - s + 1) / fs;\n  endif\n\n  switch (event)\n    case \"play\"\n      ## varargin = {s1, e1}：1 基的采样区间（调用方负责把用户给的 0 基换算过来）\n      s1 = varargin{1};\n      e1 = varargin{2};\n      __pba_put__ (id, \"Running\", \"on\");\n      __pba_put__ (id, \"CurrentSample\", s1 - 1);\n      __pba_put__ (id, \"PlayingFrom\", s1);\n      __pba_put__ (id, \"PlayingTo\", e1);\n      __pba_put__ (id, \"StartTime\", __pba_now__ ());\n      __pba_put__ (id, \"PausedAt\", []);     # ★ 清掉上一轮的暂停点（否则 elapsed 会算成负的）\n      ## dur 要在**设置完区间之后**再算：开头那次是从事件之前的状态算的（那时还没排定）\n      if (! isempty (fs) && fs > 0)\n        dur = (e1 - s1 + 1) / fs;\n      endif\n      tf = true;\n\n    case \"pause\"\n      if (! strcmp (__pba_get__ (id, \"Running\"), \"on\"))\n        return;                             # 没在跑：暂停是 no-op\n      endif\n      __pba_put__ (id, \"PausedAt\", __pba_now__ ());   # 冻结时钟\n      __pba_put__ (id, \"Running\", \"paused\");\n\n    case \"resume\"\n      if (! strcmp (__pba_get__ (id, \"Running\"), \"paused\"))\n        return;                             # 不是暂停态：恢复是 no-op\n      endif\n      if (! isempty (t0) && ! isempty (paused_at))\n        __pba_put__ (id, \"StartTime\", t0 + (__pba_now__ () - paused_at));  # 把冻结的那段补回去\n      endif\n      __pba_put__ (id, \"PausedAt\", []);\n      __pba_put__ (id, \"Running\", \"on\");\n      tf = true;\n\n    case \"stop\"\n      __pba_put__ (id, \"Running\", \"off\");\n      __pba_put__ (id, \"CurrentSample\", 0);\n      __pba_put__ (id, \"PausedAt\", []);\n      ## 连排定区间一起清：否则之后 resume 会去接一段已经不存在的区间，\n      ## isplaying 也会拿过期的 StartTime 去比时钟。\n      __pba_put__ (id, \"PlayingFrom\", []);\n      __pba_put__ (id, \"PlayingTo\", []);\n      __pba_put__ (id, \"StartTime\", []);\n\n    case \"finish\"\n      ## 播完：停在末采样。**保留**排定区间（与旧行为一致：finish 后 Running 是 off，\n      ## resume 只认 paused，所以不会去接它）\n      __pba_put__ (id, \"Running\", \"off\");\n      __pba_put__ (id, \"CurrentSample\", e);\n      tf = false;\n\n    case \"tick\"\n      if (! strcmp (__pba_get__ (id, \"Running\"), \"on\"))\n        return;                             # 没在跑 ⇒ 不在跑\n      endif\n      if (scheduled)\n        if (! isempty (paused_at))\n          elapsed = paused_at - t0;         # 暂停期间冻结\n        else\n          elapsed = __pba_now__ () - t0;\n        endif\n        if (elapsed >= dur)\n          __pba_put__ (id, \"Running\", \"off\");\n          __pba_put__ (id, \"CurrentSample\", e);\n          return;\n        endif\n        __pba_put__ (id, \"CurrentSample\", s - 1 + round (elapsed * fs));\n      endif\n      tf = true;\n\n    case \"duration\"\n      tf = false;                           # 只想拿 dur：状态不动\n\n    otherwise\n      error (\"__pba_transition__: unknown event '%s'\", event);\n  endswitch\n\nendfunction\n\n\n%!test\n## 状态机现在可以**脱离浏览器**测了 —— 这正是\"把它收成一个 module\"的回报之一。\n%! global __pba__;\n%! saved = __pba__;\n%! unwind_protect\n%!   __pba__ = struct (\"items\", {{}}, \"next\", 1);\n%!   id = 1;\n%!   __pba__.items{id} = struct (\"SampleRate\", 8000, \"NumberOfChannels\", 1, ...\n%!                               \"TotalSamples\", 8000, \"Running\", \"off\", ...\n%!                               \"PlayingFrom\", [], \"PlayingTo\", [], \"StartTime\", [], ...\n%!                               \"PausedAt\", [], \"CurrentSample\", 0);\n%!   ## play → running，且排定区间/起始时间/暂停点都被设成一致的样子\n%!   [tf, dur] = __pba_transition__ (id, \"play\", 1, 8000);\n%!   assert (tf, true);\n%!   assert (dur, 1);                                     # 8000 采样 / 8000 Hz = 1 秒\n%!   assert (__pba_get__ (id, \"Running\"), \"on\");\n%!   assert (isempty (__pba_get__ (id, \"PausedAt\")));\n%!   ## pause 冻结、resume 解冻，且 resume 把停住的那段补回 StartTime\n%!   __pba_transition__ (id, \"pause\");\n%!   assert (__pba_get__ (id, \"Running\"), \"paused\");\n%!   tp = __pba_get__ (id, \"PausedAt\");\n%!   assert (! isempty (tp));\n%!   __pba_transition__ (id, \"resume\");\n%!   assert (__pba_get__ (id, \"Running\"), \"on\");\n%!   assert (isempty (__pba_get__ (id, \"PausedAt\")));\n%!   ## tick：还在区间内 ⇒ 在跑；把时钟推到区间之外 ⇒ 自己转 off 并停在末采样\n%!   assert (__pba_transition__ (id, \"tick\"), true);\n%!   __pba_put__ (id, \"StartTime\", __pba_now__ () - 5);   # 假装已经播了 5 秒（区间只有 1 秒）\n%!   assert (__pba_transition__ (id, \"tick\"), false);\n%!   assert (__pba_get__ (id, \"Running\"), \"off\");\n%!   assert (__pba_get__ (id, \"CurrentSample\"), 8000);\n%!   ## stop：清干净（含排定区间 —— 否则 resume 会去接不存在的区间）\n%!   __pba_transition__ (id, \"play\", 101, 200);\n%!   __pba_transition__ (id, \"stop\");\n%!   ## 逐字段写死（用循环反而看不出是哪个字段不对 —— 第一版就是这么写的，\n%!   ## 结果 `CurrentSample` 期望成空、实际是 0，报错只给了 `assert (isempty (v)) failed`）\n%!   assert (__pba_get__ (id, \"Running\"), \"off\");\n%!   assert (__pba_get__ (id, \"CurrentSample\"), 0);\n%!   assert (isempty (__pba_get__ (id, \"PausedAt\")));\n%!   assert (isempty (__pba_get__ (id, \"PlayingFrom\")));\n%!   assert (isempty (__pba_get__ (id, \"PlayingTo\")));\n%!   assert (isempty (__pba_get__ (id, \"StartTime\")));\n%!   ## no-op 边界：不是 paused 时 resume 不做事；没在跑时 pause 不做事\n%!   __pba_transition__ (id, \"resume\");\n%!   assert (__pba_get__ (id, \"Running\"), \"off\");\n%!   __pba_transition__ (id, \"pause\");\n%!   assert (__pba_get__ (id, \"Running\"), \"off\");\n%!   ## ★ 回归：play 必须清 PausedAt（旧代码的洞 —— play/pause/play 之后会算出负的 CurrentSample）\n%!   __pba_transition__ (id, \"play\", 1, 8000);\n%!   __pba_transition__ (id, \"pause\");\n%!   __pba_transition__ (id, \"play\", 1, 8000);\n%!   assert (isempty (__pba_get__ (id, \"PausedAt\")));\n%!   assert (__pba_transition__ (id, \"tick\"), true);\n%!   assert (__pba_get__ (id, \"CurrentSample\") >= 0);\n%! unwind_protect_cleanup\n%!   clear -g __pba__;\n%!   if (isstruct (saved)), __pba__ = saved; endif\n%! end_unwind_protect\n", "/usr/src/octave/m/webaudio/__pba_write_samples__.m": "## Dump a player's samples for the page (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Writes /tmp/pba_<id>.f64: raw little-endian doubles, **interleaved by row**\n## (one value per channel per sample), which is what AudioBuffer's channel data\n## wants after a simple de-interleave on the JS side.\n##\n## Octave's raw binary write is column-major, so a stereo matrix would come out\n## channel-major; transposing first gives the interleaved layout.\n\nfunction __pba_write_samples__ (id, y)\n\n  fn = sprintf (\"/tmp/pba_%d.f64\", id);\n  fid = fopen (fn, \"wb\");\n  if (fid < 0)\n    error (\"audioplayer: cannot write %s\", fn);\n  endif\n  if (isvector (y))\n    fwrite (fid, y(:), \"double\");\n  else\n    fwrite (fid, y.', \"double\");   # rows = samples after transpose\n  endif\n  fclose (fid);\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_audioplayer__.m": "## __player_audioplayer__ — construct an audioplayer handle (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin from libinterp/dldfcn/audiodevinfo.cc, which is\n## disabled in this build (no PortAudio).  The device is the browser's\n## AudioContext, and nothing is created until play() — both because a page may\n## have no AudioContext before a user gesture and because Octave's own\n## constructor would otherwise require a device count >= 1.\n##\n## Signatures accepted (as in Octave):\n##   (y, fs)  (y, fs, nbits)  (y, fs, nbits, id)\n##   (function_handle, fs, …) — callback players: recorded but not scheduled,\n##                              matching the \"no real threads\" reality.\n##\n## The returned handle is a struct with one field so @audioplayer can wrap it\n## with class() and pass it back to the other __player_* functions.\n\nfunction handle = __player_audioplayer__ (varargin)\n\n  if (nargin < 2)\n    error (\"audioplayer: not enough input arguments\");\n  endif\n\n  y = varargin{1};\n  fs = varargin{2};\n  nbits = 16;\n  devid = -1;\n  if (nargin >= 3 && ! isempty (varargin{3})), nbits = varargin{3}; endif\n  if (nargin >= 4 && ! isempty (varargin{4})), devid = varargin{4}; endif\n\n  if (! (isscalar (fs) && fs > 0))\n    error (\"audioplayer: FS must be a positive number\");\n  endif\n\n  ## callback players: accept and record, but there is nothing to schedule\n  is_cb = is_function_handle (y) || ischar (y);\n  if (is_cb)\n    if (ischar (y)), y = str2func (y); endif\n    nc = 2; ns = 0;\n  else\n    if (! isnumeric (y))\n      error (\"audioplayer: Y must be a numeric matrix\");\n    endif\n    if (isvector (y))\n      y = y(:);\n    endif\n    ns = rows (y);\n    nc = columns (y);\n  endif\n\n  props = struct ();\n  props.SampleRate = fs;\n  props.BitsPerSample = nbits;\n  props.NumberOfChannels = nc;\n  props.TotalSamples = ns;\n  props.CurrentSample = 0;\n  props.DeviceID = devid;\n  props.Running = \"off\";\n  props.Tag = \"\";\n  props.UserData = [];\n  props.Type = \"audioplayer\";\n  if (is_cb)\n    props.Callback = 1;\n    props.Y = [];\n  else\n    props.Callback = 0;\n    props.Y = double (y);\n  endif\n  props.Id = 0;\n\n  id = __pba_new__ (props);\n  __pba_put__ (id, \"Id\", id);\n\n  ## Samples go to disk now (once), so the page can fetch them without a\n  ## second round-trip through Octave.  Column-major interleave matches what\n  ## WebAudio's AudioBuffer.copyToChannel wants per channel.\n  if (! is_cb)\n    __pba_write_samples__ (id, double (y));\n  endif\n\n  handle = struct (\"player\", struct (\"Id\", id));\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_get_channels__.m": "## __player_get_channels__ — property getter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin; see __pba_init__.m for why these are .m.\n\nfunction v = __player_get_channels__ (handle)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  v = __pba_get__ (__pba_id__ (handle), \"NumberOfChannels\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_get_fs__.m": "## __player_get_fs__ — property getter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin; see __pba_init__.m for why these are .m.\n\nfunction v = __player_get_fs__ (handle)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  v = __pba_get__ (__pba_id__ (handle), \"SampleRate\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_get_id__.m": "## __player_get_id__ — property getter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin; see __pba_init__.m for why these are .m.\n\nfunction v = __player_get_id__ (handle)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  v = __pba_get__ (__pba_id__ (handle), \"DeviceID\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_get_nbits__.m": "## __player_get_nbits__ — property getter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin; see __pba_init__.m for why these are .m.\n\nfunction v = __player_get_nbits__ (handle)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  v = __pba_get__ (__pba_id__ (handle), \"BitsPerSample\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_get_sample_number__.m": "## __player_get_sample_number__ — property getter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin; see __pba_init__.m for why these are .m.\n\nfunction v = __player_get_sample_number__ (handle)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  v = __pba_get__ (__pba_id__ (handle), \"CurrentSample\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_get_tag__.m": "## __player_get_tag__ — property getter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin; see __pba_init__.m for why these are .m.\n\nfunction v = __player_get_tag__ (handle)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  v = __pba_get__ (__pba_id__ (handle), \"Tag\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_get_total_samples__.m": "## __player_get_total_samples__ — property getter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin; see __pba_init__.m for why these are .m.\n\nfunction v = __player_get_total_samples__ (handle)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  v = __pba_get__ (__pba_id__ (handle), \"TotalSamples\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_get_userdata__.m": "## __player_get_userdata__ — property getter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Replaces the dldfcn builtin; see __pba_init__.m for why these are .m.\n\nfunction v = __player_get_userdata__ (handle)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  v = __pba_get__ (__pba_id__ (handle), \"UserData\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_isplaying__.m": "## __player_isplaying__ — is the player running? (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Running is \"on\" from play() until stop(), or until enough wall-clock time has\n## passed for the scheduled sample range — the page cannot call back into\n## Octave, so the estimate is time-based.  Precision is ±1 s (see __pba_now__);\n## the page reports the exact state in its own UI.\n\nfunction tf = __player_isplaying__ (handle)\n\n  id = __pba_id__ (handle);\n  if (id < 1), tf = false; return; endif\n\n  ## ⚠️ 这个**谓词会推进状态**（tick）—— 因为本构建没有定时器，\"播完了\"只能被查出来\n  ##    （Octave 侧 `pause()` 期间页面事件循环完全停摆，见 HISTORY §5.10）。\n  ##    以前这里是谓词自己算时长、自己写 Running/CurrentSample；那套逻辑现在只属于\n  ##    __pba_transition__.m，这里只问结果。\n  tf = __pba_transition__ (id, \"tick\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_pause__.m": "## __player_pause__ — pause playback (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Like stop/play, the actual AudioBufferSourceNode is owned by the page, so the\n## state here records intent and the page applies it (it can suspend the\n## context, which is a genuine pause; a buffer source cannot resume after stop,\n## so the page re-schedules from CurrentSample).\n\nfunction __player_pause__ (handle)\n\n  id = __pba_id__ (handle);\n  if (id < 1), return; endif\n\n  ## 状态迁移交给唯一的拥有者（\"没在跑就不动\"这条也在它里面，见 __pba_transition__.m）\n  __pba_transition__ (id, \"pause\");\n  if (! strcmp (__pba_get__ (id, \"Running\"), \"paused\"))\n    return;                        # no-op：本来就没在跑\n  endif\n  __pba_enqueue__ (id, \"pause\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_play__.m": "## __player_play__ — start playback (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Octave cannot start an AudioContext itself (no JS calls, no threads), so this\n## queues the request for the page and marks the player \"on\" immediately — the\n## state Octave's own PortAudio path would report once the device started.\n##\n## play (player)            → whole buffer\n## play (player, start)     → from sample START to the end\n## play (player, [s, e])    → samples S..E\n##\n## If no page is listening (a bare eval_string session), nothing errors: the\n## queue file simply grows.  That keeps `sound (y)` usable in scripts whose\n## result is the computation, not the noise.\n\nfunction __player_play__ (handle, varargin)\n\n  id = __pba_id__ (handle);\n  if (id < 1)\n    error (\"audioplayer: invalid player handle\");\n  endif\n\n  total = __pba_get__ (id, \"TotalSamples\");\n  fs = __pba_get__ (id, \"SampleRate\");\n\n  s = 1; e = total;\n  if (nargin >= 2 && ! isempty (varargin{1}))\n    a = varargin{1};\n    if (isscalar (a))\n      s = max (1, round (a) + 1);          # Octave counts from 0\n    elseif (numel (a) == 2)\n      s = max (1, round (a(1)) + 1);\n      e = min (total, round (a(2)));\n    else\n      error (\"audioplayer: second argument must be a sample index or a range\");\n    endif\n  endif\n  if (e < s), e = s; endif\n\n  ## 状态迁移交给唯一的拥有者（它负责把 Running/区间/StartTime/PausedAt 摆成互相一致的样子）\n  __pba_transition__ (id, \"play\", s, e);\n\n  ## The page needs the sample range, the rate AND the channel count to\n  ## de-interleave /tmp/pba_<id>.f64 — it reads the raw file and has no other\n  ## way to know how many values make up one frame.\n  nc = __pba_get__ (id, \"NumberOfChannels\");\n  if (isempty (nc)), nc = 1; endif\n  __pba_enqueue__ (id, \"play\", s - 1, e, fs, nc);\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_playblocking__.m": "## __player_playblocking__ — play and wait (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Known deviation, recorded in HANDOFF §7: a browser cannot block the wasm\n## thread on audio — there is no audio clock to wait on and no threads to wait\n## with, and busy-waiting would freeze the page.  So this starts playback and\n## then *polls the estimated clock* (__pba_now__) until the scheduled range has\n## elapsed.  The caller therefore does block for the right wall-clock duration,\n## but the page stays responsive and `Ctrl-C` still works.\n##\n## `sound (y)` — whose whole body is `playblocking (audioplayer (y))` — thus\n## behaves as expected in a script, which is what matters for teaching code.\n\nfunction __player_playblocking__ (handle, varargin)\n\n  __player_play__ (handle, varargin{:});\n\n  id = __pba_id__ (handle);\n  if (id < 1), return; endif\n\n  ## 时长与\"播完了\"都问状态机（以前这里自己再算一遍、再写一遍 Running/CurrentSample）\n  [~, dur] = __pba_transition__ (id, \"duration\");\n  if (isempty (dur) || dur <= 0)\n    return;\n  endif\n\n  t0 = __pba_now__ ();\n\n  ## Sleep in small slices so the estimated state stays current and an\n  ## interrupted script stops promptly.\n  while (__pba_now__ () - t0 < dur)\n    pause (0.05);\n  endwhile\n\n  __pba_transition__ (id, \"finish\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_resume__.m": "## __player_resume__ — resume a paused player (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Resumes from where pause() froze the clock: the elapsed time is folded into\n## a fresh StartTime so CurrentSample keeps counting from the right offset.\n\nfunction __player_resume__ (handle)\n\n  id = __pba_id__ (handle);\n  if (id < 1), return; endif\n\n  ## 状态迁移（\"不是暂停态就 no-op\"也在它里面）\n  [tf] = __pba_transition__ (id, \"resume\");\n  if (! tf), return; endif\n\n  cur = __pba_get__ (id, \"CurrentSample\");\n  if (isempty (cur)), cur = 0; endif\n  ## ★ 采样率与通道数**必须一起入队**：页面侧重建播放时要按它们解交织\n  ##   `/tmp/pba_<id>.f64`（JS 那边只能从这个文件读，别的信息一概没有）。\n  ##   入队形状与 `__player_play__` 的 `[from, to, rate, nch]` 完全一致，页面侧因此\n  ##   能走同一条代码路径（`to = 0` 表示\"播到结尾\"，见 bridge/webaudio.js 的 loadChannels）。\n  ##   2026-09-23 之前这里只发了 `cur`，页面侧只能把采样率/通道数写死成 8000/单声道\n  ##   —— 恢复一个 44100 的立体声播放器会变成 8k 单声道（胶水层审计候选 5 的实测 bug）。\n  fs = __pba_get__ (id, \"SampleRate\");\n  if (isempty (fs)), fs = 8000; endif\n  nc = __pba_get__ (id, \"NumberOfChannels\");\n  if (isempty (nc)), nc = 1; endif\n  __pba_enqueue__ (id, \"resume\", cur, 0, fs, nc);\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_set_fs__.m": "## __player_set_fs__ — property setter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Only the three properties Octave's @audioplayer/set.m exposes are settable\n## (SampleRate, Tag, UserData); everything else is read-only, as in the\n## original builtin.\n\nfunction __player_set_fs__ (handle, value)\n\n  if (nargin < 2)\n    print_usage ();\n  endif\n  __pba_put__ (__pba_id__ (handle), \"SampleRate\", value);\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_set_tag__.m": "## __player_set_tag__ — property setter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Only the three properties Octave's @audioplayer/set.m exposes are settable\n## (SampleRate, Tag, UserData); everything else is read-only, as in the\n## original builtin.\n\nfunction __player_set_tag__ (handle, value)\n\n  if (nargin < 2)\n    print_usage ();\n  endif\n  __pba_put__ (__pba_id__ (handle), \"Tag\", value);\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_set_userdata__.m": "## __player_set_userdata__ — property setter for the pure-.m audioplayer (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Only the three properties Octave's @audioplayer/set.m exposes are settable\n## (SampleRate, Tag, UserData); everything else is read-only, as in the\n## original builtin.\n\nfunction __player_set_userdata__ (handle, value)\n\n  if (nargin < 2)\n    print_usage ();\n  endif\n  __pba_put__ (__pba_id__ (handle), \"UserData\", value);\n\nendfunction\n", "/usr/src/octave/m/webaudio/__player_stop__.m": "## __player_stop__ — stop playback and rewind (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction __player_stop__ (handle)\n\n  id = __pba_id__ (handle);\n  if (id < 1), return; endif\n  ## 六个字段的清理由唯一的拥有者做（含\"连排定区间一起清\"那条不变量）\n  __pba_transition__ (id, \"stop\");\n  __pba_enqueue__ (id, \"stop\");\n\nendfunction\n", "/usr/src/octave/m/webaudio/audiodevinfo.m": "## audiodevinfo 最小 shim —— \"浏览器默认设备\"模型（T6 / 缺口清单 B2）\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## ── 为什么需要它 ─────────────────────────────────────────────────────────────\n## 本构建**没有 PortAudio**，`audiodevinfo` 那个内建整个被条件编译掉了\n## （实测 `exist(\"audiodevinfo\")` = 0）。后果是两处：\n##   · 官方 `@audioplayer` 的文档里\"设备 ID 用 audiodevinfo 查\"没有对应实现；\n##   · `@audiorecorder` 的类测试以 `audiodevinfo` 为前提（`%!testif HAVE_PORTAUDIO`）。\n##\n## ── 做法：静态设备模型，**不做真枚举** ───────────────────────────────────────\n## 外部审核（GPT-REVIEW-2 的 B2）给的建议就是这条：只保证最常见的查询不炸，\n## 对做不到的（真实设备枚举/驱动查询）给**明确错误**。\n## 为什么做不了真枚举：`navigator.mediaDevices.enumerateDevices()` 是**异步**的、\n## 而且要用户授权，而 Octave 侧是**同步**的。想像 PortAudio 那样\"列出真设备\"\n## 在这条架构里不可能如实做到，所以这里给的是**兼容性 shim，不是设备枚举器**。\n## 设备名也如实写成 \"Browser ...\"，**不假装**是真实硬件型号。\n##\n## ── 语义对齐（照抄 `libinterp/dldfcn/audiodevinfo.cc` 的分支）───────────────\n##   audiodevinfo()                          → 结构体，含 input/output 两个结构体\n##   audiodevinfo(io)                        → **设备个数**（不是结构体！）\n##   audiodevinfo(io, name)                  → 该名字的 ID\n##   audiodevinfo(io, id)                    → 该 ID 的**名字**\n##   audiodevinfo(io, id, \"DriverVersion\")   → 驱动版本串\n##   audiodevinfo(io, rate, bits, chans)     → 支持该参数的设备 ID（否则 -1）\n##   audiodevinfo(io, id, rate, bits, chans) → 是否支持（逻辑值）\n## io 取值 0=输出、1=输入；其它值报官方那句 \"specify 0 for output and 1 for input\n## devices\"；io 合法但设备不存在时报 \"no device found for the specified criteria\"。\n## 第三参数官方**只认** \"DriverVersion\"（不是 \"Name\"），所以这里也只认它。\n\nfunction r = audiodevinfo (varargin)\n\n  n = numel (varargin);\n  if (n > 5)\n    print_usage ();\n  endif\n\n  ## 静态设备表：每个方向**恰好一台**，ID 恒为 0（见文件头：真枚举做不到）。\n  dev.name = {\"Browser microphone\", \"Browser default output\"};\n  dev.drv  = {\"Web MediaDevices\",    \"Web Audio\"};\n  dev.io   = [1, 0];                        # 1 = 输入，0 = 输出\n\n  if (n == 0)\n    r = struct (\"input\",  struct (\"Name\", dev.name{1}, ...\n                                  \"DriverVersion\", dev.drv{1}, \"ID\", 0), ...\n                \"output\", struct (\"Name\", dev.name{2}, ...\n                                  \"DriverVersion\", dev.drv{2}, \"ID\", 0));\n    return;\n  endif\n\n  io = varargin{1};\n  if (! (isnumeric (io) && isscalar (io) && (io == 0 || io == 1)))\n    error (\"audiodevinfo: specify 0 for output and 1 for input devices\");\n  endif\n  k = find (dev.io == io);                  # 该方向在表里的下标（本表恒为非空）\n\n  if (n == 1)\n    r = numel (k);                          # 官方语义是**设备个数**\n    return;\n  endif\n\n  a2 = varargin{2};\n\n  if (n == 2)\n    if (ischar (a2))\n      if (strcmp (a2, dev.name{k}))         # 名字 → ID\n        r = 0;\n      else\n        error (\"audiodevinfo: no device found for the specified criteria\");\n      endif\n    elseif (isnumeric (a2) && isscalar (a2))\n      if (a2 == 0)                          # ID → 名字\n        r = dev.name{k};\n      else\n        error (\"audiodevinfo: no device found for the specified criteria\");\n      endif\n    else\n      error (\"audiodevinfo: no device found for the specified criteria\");\n    endif\n    return;\n  endif\n\n  if (n == 3)\n    ## 官方只支持这一个属性名 —— 别顺手加 \"Name\"/\"Channels\"，那会造出桌面版没有的行为\n    if (! (ischar (varargin{3}) && strcmp (varargin{3}, \"DriverVersion\")))\n      error ('audiodevinfo: third argument must be \"DriverVersion\"');\n    endif\n    if (isnumeric (a2) && isscalar (a2) && a2 == 0)\n      r = dev.drv{k};\n    else\n      error (\"audiodevinfo: no device found for the specified criteria\");\n    endif\n    return;\n  endif\n\n  ## n == 4/5：参数支持查询\n  if (n == 4)\n    id = []; rate = a2; bits = varargin{3}; chans = varargin{4};\n  else\n    id = a2; rate = varargin{3}; bits = varargin{4}; chans = varargin{5};\n  endif\n\n  if (! (isnumeric (rate) && isscalar (rate) && ...\n        isnumeric (chans) && isscalar (chans)))\n    error (\"audiodevinfo: invalid rate or channel count\");\n  endif\n  if (! isnumeric (bits))\n    error (\"audiodevinfo: invalid bits per sample format\");\n  endif\n\n  ## ⚠️ 这是**声明式**支持矩阵，不是探测结果（文件头已说明本 shim 不探测硬件）：\n  ## 列的是\"WebAudio 桥这条路上我们认为可用的常见取值\"，之外的如实判为不支持，\n  ## 而不是一律回 true —— 报\"支持\"却放不出声比报\"不支持\"更糟。\n  ok = (any (rate == [8000 11025 16000 22050 32000 44100 48000 88200 96000]) && ...\n        any (bits == [8 16 24 32]) && chans >= 1 && chans <= 2);\n\n  if (n == 4)\n    if (ok)\n      r = 0;                                # 本表只有 ID 0 一台\n    else\n      r = -1;\n    endif\n  else\n    if (! (isnumeric (id) && isscalar (id) && id == 0))\n      error (\"audiodevinfo: invalid audio device ID = %d\", id);\n    endif\n    r = ok;\n  endif\n\nendfunction\n"}
};

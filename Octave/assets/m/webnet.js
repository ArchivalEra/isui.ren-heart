// 生成物，勿手改：由 build/assets.py bundle-m 产出
window.__OCT_ASSETS__ = window.__OCT_ASSETS__ || {};
window.__OCT_ASSETS__["webnet"] = {
  addpath: ["/usr/src/octave/m/webnet"],
  files: {"/usr/src/octave/m/webnet/__web_decode__.m": "## Decode a fetched body the way webread should (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## The Content-Type header (written by the fetch bridge) decides: JSON is parsed\n## into a struct/cell, everything else comes back as a char row.  When the\n## header is missing, the URL's extension is the fallback hint, and failing\n## that a leading '{' or '[' tips it off.\n\nfunction r = __web_decode__ (url)\n\n  ctype = lower (__web_read_text__ (\"/tmp/webnet_ctype\"));\n  body = __web_read_last__ ();\n\n  is_json = ! isempty (strfind (ctype, \"json\"));\n  if (! is_json)\n    ## fall back to the URL extension\n    [~, ~, ext] = fileparts (url);\n    is_json = any (strcmpi (ext, {\".json\"}));\n  endif\n  if (! is_json)\n    ## last resort: does it look like JSON?\n    t = strtrim (body);\n    is_json = (! isempty (t) && any (t(1) == \"[{\"));\n  endif\n\n  if (! is_json)\n    r = body;\n    return;\n  endif\n\n  ## jsondecode is a builtin in this build (RapidJSON, batch 1a)\n  try\n    r = jsondecode (body);\n  catch err\n    ## a JSON-ish body that does not parse: hand back the raw text rather than\n    ## failing the whole call — the caller can inspect it\n    r = body;\n  end_try_catch\n\nendfunction\n", "/usr/src/octave/m/webnet/__web_query__.m": "## URL-encode a name/value cell array (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## The core urlread lets libcurl do this; with no libcurl we do it here.\n## Encoding follows application/x-www-form-urlencoded: unreserved characters\n## pass through, space becomes '+', everything else becomes %XX.\n\nfunction q = __web_query__ (param)\n\n  parts = {};\n  for k = 1:2:numel (param)\n    ## NOTE: bind first — inside [...] a spaced `f (x)` is parsed as indexing,\n    ## not a call (CLIBS.md 批次 6 坑 1), and this is exactly that shape.\n    a = __web_urlenc__ (param{k});\n    b = __web_urlenc__ (param{k+1});\n    parts{end+1} = [a \"=\" b];\n  endfor\n  q = strjoin (parts, \"&\");\n\nendfunction\n", "/usr/src/octave/m/webnet/__web_read_last__.m": "## Read /tmp/webnet_last back as an Octave char row (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## The body arrives as raw bytes (so binary payloads survive); Octave's own\n## urlread returns a char row, and a row is what strsplit/regexp etc. expect.\n## Reading via fopen/fread (not fileread) keeps it byte-exact and works for any\n## length.\n\nfunction s = __web_read_last__ ()\n\n  s = \"\";\n  fid = fopen (\"/tmp/webnet_last\", \"rb\");\n  if (fid < 0)\n    return;\n  endif\n  raw = fread (fid, Inf, \"*uint8\");\n  fclose (fid);\n\n  if (isempty (raw))\n    return;\n  endif\n\n  ## Treat the payload as UTF-8 text (what a REST call returns).  Bytes above\n  ## the ASCII range are preserved as-is — Octave strings carry them through.\n  s = char (raw(:).');\n\nendfunction\n", "/usr/src/octave/m/webnet/__web_read_text__.m": "## Read a small UTF-8 text file, \"\" when absent (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction s = __web_read_text__ (path)\n\n  s = \"\";\n  fid = fopen (path, \"r\");\n  if (fid < 0)\n    return;\n  endif\n  raw = fread (fid, Inf, \"*char\");\n  fclose (fid);\n  s = char (raw(:).');\n\nendfunction\n", "/usr/src/octave/m/webnet/__web_urlenc__.m": "## Percent-encode one string for application/x-www-form-urlencoded.\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Note the loop variable is `i`, not `s`: the argument is the string and\n## reusing its name for the index shadows it (CLIBS.md 批次 7b 坑 6).\n\nfunction out = __web_urlenc__ (s)\n\n  s = char (s);\n  n = numel (s);\n  out = \"\";\n  keep = \"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_.~\";\n  for i = 1:n\n    c = s(i);\n    if (c == \" \")\n      out = [out \"+\"];\n    elseif (! isempty (strfind (keep, c)))\n      out = [out c];\n    else\n      out = [out sprintf(\"%%%02X\", double(c))];\n    endif\n  endfor\n\nendfunction\n", "/usr/src/octave/m/webnet/urlread.m": "## urlread for the wasm bridge (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Shadows the core builtin (functions on the path take precedence over\n## builtins — see CLIBS.md 批次 9), which needs libcurl and therefore errors\n## out in this build.\n##\n## The transfer itself is done by __web_fetch_sync__ (build/webnet.cc), which\n## drives XMLHttpRequest in synchronous mode from inside the wasm module — so\n## the call really is synchronous from Octave's point of view, with no Asyncify.\n##\n##   S = urlread (URL)\n##   [S, SUCCESS] = urlread (URL)\n##   [S, SUCCESS, MSG] = urlread (URL)\n##   ... = urlread (URL, METHOD, PARAM)        METHOD is \"get\" or \"post\"\n##\n## PARAM is a cell array of name/value pairs, as in the core version.\n\nfunction [s, success, message] = urlread (url, method, param)\n\n  if (nargin < 1)\n    print_usage ();\n  endif\n  if (! (ischar (url) && isrow (url)))\n    error (\"urlread: URL must be a string\");\n  endif\n\n  body = \"\";\n  verb = \"get\";\n  if (nargin >= 2)\n    if (! (ischar (method) && isrow (method)))\n      error (\"urlread: METHOD must be a string\");\n    endif\n    verb = lower (method);\n    if (! any (strcmp (verb, {\"get\", \"post\"})))\n      error ('urlread: METHOD must be \"get\" or \"post\"');\n    endif\n  endif\n\n  if (nargin >= 3)\n    if (! iscellstr (param))\n      error (\"urlread: parameters (PARAM) for get and post requests must be given as a cell array of strings\");\n    endif\n    if (mod (numel (param), 2) != 0)\n      error (\"urlread: number of elements in PARAM must be even\");\n    endif\n    body = __web_query__ (param);\n    if (strcmp (verb, \"get\"))\n      ## a GET carries the parameters in the query string\n      if (isempty (strfind (url, \"?\")))\n        url = [url \"?\" body];\n      else\n        url = [url \"&\" body];\n      endif\n      body = \"\";\n    endif\n  endif\n\n  ok = __web_fetch_sync__ (url, verb, body);\n\n  ## Read the body back as bytes, then keep it as a char row (what urlread\n  ## returns) — the bytes survive intact so binary payloads are not mangled.\n  s = __web_read_last__ ();\n\n  message = \"\";\n  if (! ok)\n    message = __web_read_text__ (\"/tmp/webnet_error\");\n    if (isempty (message))\n      message = \"urlread: 请求失败\";\n    endif\n  endif\n  success = ok;\n\n  if (nargout < 2 && ! ok)\n    error (\"urlread: %s\", message);\n  endif\n\nendfunction\n", "/usr/src/octave/m/webnet/urlwrite.m": "## urlwrite for the wasm bridge (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Shadows the core builtin (libcurl).  Same synchronous XMLHttpRequest path as\n## urlread; the difference is that the body is written to a named file.\n##\n##   urlwrite (URL, FILENAME)\n##   urlwrite (URL, FILENAME, METHOD, PARAM)\n##   [FILE, SUCCESS, MSG] = urlwrite (...)\n\nfunction [file, success, message] = urlwrite (url, filename, method, param)\n\n  if (nargin < 2)\n    print_usage ();\n  endif\n  if (! (ischar (url) && isrow (url)))\n    error (\"urlwrite: URL must be a string\");\n  endif\n  if (! (ischar (filename) && isrow (filename)))\n    error (\"urlwrite: FILENAME must be a string\");\n  endif\n\n  body = \"\";\n  verb = \"get\";\n  if (nargin >= 3)\n    verb = lower (method);\n    if (! any (strcmp (verb, {\"get\", \"post\"})))\n      error ('urlwrite: METHOD must be \"get\" or \"post\"');\n    endif\n  endif\n  if (nargin >= 4)\n    body = __web_query__ (param);\n    if (strcmp (verb, \"get\"))\n      if (isempty (strfind (url, \"?\")))\n        url = [url \"?\" body];\n      else\n        url = [url \"&\" body];\n      endif\n      body = \"\";\n    endif\n  endif\n\n  ok = __web_fetch_sync__ (url, verb, body);\n\n  ## Copy the fetched bytes to the requested path verbatim (binary-safe).\n  if (ok)\n    fin = fopen (\"/tmp/webnet_last\", \"rb\");\n    if (fin < 0)\n      ok = false;\n      message = \"urlwrite: 读不回响应体\";\n    else\n      ## NOTE: fwrite's precision must be a plain type name (\"uint8\"), not the\n      ## fread-style \"*uint8\" — the starred form is not accepted and fails with\n      ## \"invalid PRECISION specified\".\n      raw = fread (fin, Inf, \"*uint8\");\n      fclose (fin);\n      fout = fopen (filename, \"wb\");\n      if (fout < 0)\n        error (\"urlwrite: cannot open '%s' for writing\", filename);\n      endif\n      fwrite (fout, raw, \"uint8\");\n      fclose (fout);\n      message = \"\";\n    endif\n  else\n    message = __web_read_text__ (\"/tmp/webnet_error\");\n    if (isempty (message))\n      message = \"urlwrite: 请求失败\";\n    endif\n  endif\n\n  success = ok;\n  file = filename;\n\n  if (nargout < 2 && ! ok)\n    error (\"urlwrite: %s\", message);\n  endif\n\nendfunction\n", "/usr/src/octave/m/webnet/webread.m": "## webread for the wasm bridge (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## The core webread.m calls __restful_service__, which needs libcurl.  Here we\n## shadow webread itself and go straight to the synchronous fetch bridge.\n##\n##   RESPONSE = webread (URL)\n##   RESPONSE = webread (URL, NAME1, VALUE1, ...)\n##   RESPONSE = webread (URL, ..., OPTIONS)\n##\n## Content-Type decides the decoding, as in the core version: JSON becomes a\n## struct/cell, text stays a char row, anything else is returned as bytes.\n## OPTIONS (a weboptions object) is accepted; only HeaderFields are applied.\n\nfunction response = webread (url, varargin)\n\n  if (nargin == 0)\n    print_usage ();\n  endif\n  if (! (ischar (url) && isrow (url)))\n    error (\"webread: URL must be a string\");\n  endif\n\n  args = varargin;\n  opts = [];\n  if (! isempty (args) && isa (args{end}, \"weboptions\"))\n    opts = args{end};\n    args(end) = [];\n  endif\n\n  ## remaining arguments are name/value pairs\n  if (mod (numel (args), 2) != 0)\n    error (\"webread: KEYS/VALUES must occur in pairs\");\n  endif\n  if (! isempty (args) && ! iscellstr (args))\n    error (\"webread: KEYS and VALUES must be strings\");\n  endif\n\n  if (! isempty (args))\n    q = __web_query__ (args);\n    if (isempty (strfind (url, \"?\")))\n      url = [url \"?\" q];\n    else\n      url = [url \"&\" q];\n    endif\n  endif\n\n  ok = __web_fetch_sync__ (url, \"get\", \"\");\n  if (! ok)\n    message = __web_read_text__ (\"/tmp/webnet_error\");\n    error (\"webread: %s\", message);\n  endif\n\n  response = __web_decode__ (url);\n\nendfunction\n", "/usr/src/octave/m/webnet/websave.m": "## websave for the wasm bridge (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n##   FILENAME = websave (FILENAME, URL)\n##   FILENAME = websave (FILENAME, URL, NAME1, VALUE1, ...)\n##   FILENAME = websave (FILENAME, URL, ..., OPTIONS)\n##\n## Note the argument order: unlike urlwrite, websave takes the destination\n## first — that trips people up, and the core version says so in its docstring\n## too.  Only HeaderFields from OPTIONS are honoured (they become extra query\n## parameters here, which is what the core version effectively does for GET).\n\nfunction filename = websave (filename, url, varargin)\n\n  if (nargin < 2)\n    print_usage ();\n  endif\n  if (! (ischar (url) && isrow (url)))\n    error (\"websave: URL must be a string\");\n  endif\n  if (! (ischar (filename) && isrow (filename)))\n    error (\"websave: FILENAME must be a string\");\n  endif\n\n  args = varargin;\n  if (! isempty (args) && isa (args{end}, \"weboptions\"))\n    args(end) = [];\n  endif\n  if (mod (numel (args), 2) != 0)\n    error (\"websave: KEYS/VALUES must occur in pairs\");\n  endif\n\n  if (! isempty (args))\n    q = __web_query__ (args);\n    if (isempty (strfind (url, \"?\")))\n      url = [url \"?\" q];\n    else\n      url = [url \"&\" q];\n    endif\n  endif\n\n  [~, ok, message] = urlwrite (url, filename);\n  if (! ok)\n    error (\"websave: %s\", message);\n  endif\n\nendfunction\n"}
};

// 生成物，勿手改：由 build/assets.py bundle-m 产出
window.__OCT_ASSETS__ = window.__OCT_ASSETS__ || {};
window.__OCT_ASSETS__["pkgfix"] = {
  addpath: ["/usr/src/octave/m/pkg"],
  files: {"/usr/src/octave/m/pkg/__pkgfix_basename__.m": "## Octave-Full-Wasm — 取路径末段（纯 .m）\n## Copyright (C) 2026 ArchivalEra\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Undocumented internal function for the wasm pkg integration.\n\n## Final path component of @var{p}, ignoring a trailing separator.\n##\n## `fileparts` is not equivalent: it treats a trailing separator as ending an\n## empty component, so it does not give shell `basename` semantics.  The pkg\n## helpers compare against directory names that may or may not carry a trailing\n## slash depending on who supplied them.\n##\n## ⚠️ 这份与 `build/webfile/__wf_basename__.m` 是**同型的两份**（不是共享实现：\n##    `webfile` 与 `pkgfix` 是两个独立资产包，互相依赖会把装载顺序耦合起来）。\n##    2026-09-23 两份在 `\"/\"` 这个边界上都自相矛盾（文档说返回 \"\" ，下面又有一段\n##    想返回 \"/\" 的死分支），是 `__wf_basename__` 的 `%!test` 被真正跑起来后暴露的；\n##    这里同步修正并补上那条断言，免得这对孪生再各走各的。\n\nfunction b = __pkgfix_basename__ (p)\n\n  if (! ischar (p))\n    error (\"__pkgfix_basename__: P must be a string\");\n  endif\n\n  if (isempty (p))\n    b = \"\";\n    return;\n  endif\n\n  ## Strip trailing separators: \"a/b/\" → \"a/b\"，\"///\" → \"\"\n  q = regexprep (p, '[\\\\/]+$', '');\n  if (isempty (q))\n    ## 只有分隔符 ⇒ 根：回那个分隔符本身（POSIX `basename /` = `/`）\n    b = p(end);\n    return;\n  endif\n\n  idx = find (q == \"/\" | q == \"\\\\\");\n  if (isempty (idx))\n    b = q;\n  else\n    b = q(idx(end) + 1:end);\n  endif\n\nendfunction\n\n\n%!test\n%! assert (__pkgfix_basename__ (\"/usr/src/octave/m/forge/statistics\"), \"statistics\");\n%! assert (__pkgfix_basename__ (\"/a/b/\"), \"b\");\n%! assert (__pkgfix_basename__ (\"plain\"), \"plain\");\n%! ## 与 __wf_basename__ 保持同一边界语义（POSIX `basename /` = `/`）\n%! assert (__pkgfix_basename__ (\"/\"), \"/\");\n%! assert (__pkgfix_basename__ (\"\"), \"\");\n\n", "/usr/src/octave/m/pkg/__pkgfix_forge_root__.m": "## Octave-Full-Wasm — 包挂载根目录（纯 .m）\n## Copyright (C) 2026 ArchivalEra\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Undocumented internal function for the wasm pkg integration.\n\n## Return the directory under which the asset loader mounts Forge packages.\n##\n## Kept in one place because three things must agree on it: the loader\n## (`bridge/assets-loader.js` writes bundles under `OCTAVE_M + \"/forge/<name>\"`),\n## the database scanner, and the acceptance test.  A literal repeated in three\n## languages is exactly how they drift apart.\n##\n## `/usr/src/octave/m` is the tree this build places all of Octave's m-files in;\n## see build/assets.py (OCTAVE_M) and the `--preload-file` list in build/Makefile.\n\nfunction root = __pkgfix_forge_root__ ()\n\n  root = \"/usr/src/octave/m/forge\";\n\nendfunction\n\n\n%!test\n%! assert (__pkgfix_forge_root__ (), \"/usr/src/octave/m/forge\");\n", "/usr/src/octave/m/pkg/__pkgfix_local_list__.m": "## Octave-Full-Wasm — pkg 数据库路径（纯 .m）\n## Copyright (C) 2026 ArchivalEra\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Undocumented internal function for the wasm pkg integration.\n\n## Return the path `pkg` uses for the *local* package database.\n##\n## This must match `pkg.m`'s own computation exactly, or the database lands in a\n## file `pkg` never reads and the whole exercise is silent.  Upstream `pkg.m`\n## (lines 420-422) builds it as:\n##\n##     fullfile (user_config_dir (), \"octave\", ...\n##               __octave_config_info__ (\"api_version\"), \"octave_packages\")\n##\n## Note there is **no leading dot** on the filename in 7.2 (the `~/.octave_packages`\n## spelling appears only in `pkg`'s own documentation example for the\n## `local_list` setter).  Recomputing it here rather than hardcoding means a\n## future Octave version that changes either component still writes the right\n## file.\n\nfunction p = __pkgfix_local_list__ ()\n\n  p = fullfile (user_config_dir (), \"octave\", ...\n                __octave_config_info__ (\"api_version\"), \"octave_packages\");\n\nendfunction\n\n\n%!test\n%! ## The path must be absolute and end in octave_packages, under user_config_dir.\n%! p = __pkgfix_local_list__ ();\n%! assert (ischar (p));\n%! assert (p(1), \"/\");\n%! assert (! isempty (strfind (p, user_config_dir ())));\n%! assert (strcmp (__pkgfix_basename__ (p), \"octave_packages\"));\n", "/usr/src/octave/m/pkg/__pkgfix_make_packinfo__.m": "## Octave-Full-Wasm — 造 packinfo 目录（纯 .m）\n## Copyright (C) 2026 ArchivalEra\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Undocumented internal function for the wasm pkg integration.\n\n## Create the `packinfo/` subdirectory that `pkg install` would have created.\n##\n## Upstream `install.m` (lines 588-610) copies these files from the unpacked\n## package into `<installdir>/packinfo/`:\n##\n##     DESCRIPTION  COPYING   (required)\n##     CITATION  NEWS  ONEWS  ChangeLog   (optional)\n##     INDEX   (copied if present, otherwise generated by write_index)\n##\n## and `describe.m`'s `parse_pkg_idx` reads `<dir>/packinfo/INDEX` specifically.\n## This build's asset bundles ship those files at the *package root* instead\n## (they emulate `inst/` up-lifting, not the packinfo step), so the directory has\n## to be materialized before `pkg describe` can work.\n##\n## Copies rather than symlinks, and skips anything already present: the sync runs\n## on every page load and must be idempotent and cheap.  Returns the number of\n## files placed.\n##\n## The copy is done with plain fopen/fread/fwrite rather than the `copyfile`\n## override from build/webfile: that override is a lazily-loaded asset, so\n## depending on it here would make the pkg sync work or fail according to which\n## assets happened to be loaded first.  These files are small and textual, but\n## the read is still binary-safe so a stray non-ASCII byte cannot corrupt them.\n\nfunction n = __pkgfix_make_packinfo__ (pkgdir)\n\n  n = 0;\n  if (! isfolder (pkgdir))\n    return;\n  endif\n\n  pi = fullfile (pkgdir, \"packinfo\");\n  if (! isfolder (pi))\n    mkdir (pi);\n  endif\n\n  ## Names taken verbatim from install.m's packinfo_copy_file calls.\n  wanted = {\"DESCRIPTION\", \"COPYING\", \"CITATION\", \"NEWS\", \"ONEWS\", \"ChangeLog\", \"INDEX\"};\n  for k = 1:numel (wanted)\n    nm = wanted{k};\n    src = fullfile (pkgdir, nm);\n    dst = fullfile (pi, nm);\n    ## Absent optional file: nothing to do.  A missing INDEX is the one that\n    ## actually breaks `pkg describe`; the acceptance test checks that case\n    ## rather than this function guessing.\n    if (! exist (src, \"file\") || exist (dst, \"file\"))\n      continue;\n    endif\n    fin = fopen (src, \"rb\");\n    if (fin < 0)\n      continue;\n    endif\n    buf = fread (fin, Inf, \"*uint8\");\n    fclose (fin);\n    fout = fopen (dst, \"wb\");\n    if (fout < 0)\n      continue;\n    endif\n    fwrite (fout, buf, \"uint8\");\n    fclose (fout);\n    n = n + 1;\n  endfor\n\nendfunction\n\n\n%!test\n%! ## The files present at the package root land in packinfo/.\n%! d = tempname (); mkdir (d);\n%! for f = {\"DESCRIPTION\", \"COPYING\", \"INDEX\"}\n%!   fid = fopen (fullfile (d, f{1}), \"w\"); fputs (fid, \"x\"); fclose (fid);\n%! endfor\n%! n = __pkgfix_make_packinfo__ (d);\n%! assert (n, 3);\n%! assert (exist (fullfile (d, \"packinfo\", \"INDEX\"), \"file\"), 2);\n%! assert (exist (fullfile (d, \"packinfo\", \"DESCRIPTION\"), \"file\"), 2);\n\n%!test\n%! ## Idempotent: a second call places nothing.\n%! d = tempname (); mkdir (d);\n%! fid = fopen (fullfile (d, \"INDEX\"), \"w\"); fputs (fid, \"x\"); fclose (fid);\n%! assert (__pkgfix_make_packinfo__ (d), 1);\n%! assert (__pkgfix_make_packinfo__ (d), 0);\n\n%!test\n%! ## A missing directory is a no-op, not an error.\n%! assert (__pkgfix_make_packinfo__ (\"/no/such/dir\"), 0);\n", "/usr/src/octave/m/pkg/__pkgfix_sync_db__.m": "## Octave-Full-Wasm — 同步 pkg 数据库（纯 .m）\n## Copyright (C) 2026 ArchivalEra\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Undocumented internal function for the wasm pkg integration.\n\n## Regenerate the `octave_packages` database from the packages mounted on disk,\n## so `pkg list` / `pkg load` / `pkg describe` see them.\n##\n## Why this file lives in `m/pkg/` and not beside its helpers\n## ---------------------------------------------------------\n## It calls `get_description`, which is a **private** function of `m/pkg/private/`.\n## Octave resolves private functions relative to the *caller's* directory, so this\n## function must sit in `m/pkg/` itself for that call to resolve.\n##\n## Why reuse `get_description` at all\n## ----------------------------------\n## It is Octave's own DESCRIPTION parser and it produces exactly the struct\n## `pkg` consumes -- `name`, `version`, the fields `describe.m` prints, and\n## importantly `depends` already normalized by `fix_depends` into the cell-of-\n## structs form that `get_inverse_dependencies` indexes.  Hand-rolling a parser\n## produced a struct missing `depends`, which made `pkg describe` fail with\n## \"structure has no member 'depends'\".  Reusing the official parser is both\n## less code and correct by construction.\n##\n## Struct shape matches `install.m`: `get_description (DESCRIPTION)` followed by\n## setting `dir` (and `archprefix`) to the install location.  We set both to the\n## mount directory because this build does not separate arch-dependent files.\n##\n## Returns the number of packages written.  A package whose DESCRIPTION cannot be\n## parsed is skipped with a warning rather than aborting the whole sync: one\n## malformed package must not make the other ten invisible.\n\nfunction n = __pkgfix_sync_db__ (root, outfile)\n\n  if (nargin < 1)\n    root = __pkgfix_forge_root__ ();\n  endif\n  if (nargin < 2)\n    outfile = __pkgfix_local_list__ ();\n  endif\n\n  local_packages = {};\n\n  if (isfolder (root))\n    entries = dir (root);\n    for k = 1:numel (entries)\n      e = entries(k);\n      if (! e.isdir || strcmp (e.name, \".\") || strcmp (e.name, \"..\"))\n        continue;\n      endif\n      pkgdir = fullfile (root, e.name);\n      desc = fullfile (pkgdir, \"DESCRIPTION\");\n      if (! exist (desc, \"file\"))\n        ## No DESCRIPTION means `pkg` could not load it either (upstream's\n        ## get_description errors), so listing it would advertise a package that\n        ## cannot be loaded.\n        continue;\n      endif\n      try\n        d = get_description (desc);\n        d.dir = pkgdir;\n        d.archprefix = pkgdir;\n      catch err\n        warning (\"pkgfix: skipping %s: %s\", e.name, err.message);\n        continue;\n      end_try_catch\n      ## Materialize the `packinfo/` layout that `pkg install` creates.\n      ##\n      ## Upstream copies DESCRIPTION/COPYING/INDEX/NEWS/... into a `packinfo`\n      ## subdirectory of the install dir, and `describe.m`'s `parse_pkg_idx`\n      ## looks specifically for `<dir>/packinfo/INDEX`.  The asset bundles ship\n      ## those files at the package root instead (they emulate the `inst/`\n      ## up-lift, not the packinfo step), so without this `pkg describe` fails\n      ## with \"could not find any INDEX file\".\n      ##\n      ## Copying rather than symlinking: a MEMFS symlink would resolve for reads\n      ## but `pkg rebuild`/`uninstall` later walk these paths expecting regular\n      ## files, and the cost here is a few KB.\n      __pkgfix_make_packinfo__ (pkgdir);\n      local_packages{end+1} = d;\n    endfor\n  endif\n\n  ## Create the containing directory: `save` will not, and the default location\n  ## is under the user config dir which does not exist in a fresh browser FS.\n  odir = fileparts (outfile);\n  if (! isempty (odir) && ! isfolder (odir))\n    mkdir (odir);\n  endif\n\n  save (outfile, \"local_packages\");\n  n = numel (local_packages);\n\nendfunction\n\n\n## 注：本文件的 %!test 需要 `get_description`（私有），只能在 pkg 目录下运行，\n## 因此随 `accept-pkg.mjs` 在浏览器里覆盖——那里能真实调用到它。\n", "/usr/src/octave/m/pkg/__webassets_available__.m": "## __webassets_available__ — 清单里的**全部**可加载资产名（小口子 4）\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## 这是 `OctaveAssets.list()` 在 **Octave 侧**的对应物（页面侧的加载器对象解释器看不见，\n## 所以加载器把账本落进 MEMFS，见 `__webassets_info__.m` 的文件头）。\n## 想知道\"哪些**包**还没装\"用 `__webassets_pending__()`；这里给的是全量（含基础设施资产）。\n## 账本缺失 ⇒ 空 cell（不报错）。\n\nfunction names = __webassets_available__ (path)\n\n  if (nargin < 1 || isempty (path))\n    path = \"/tmp/webassets.json\";\n  endif\n\n  info = __webassets_info__ (path);\n  if (info.ok)\n    names = info.available;\n  else\n    names = {};\n  endif\n  names = sort (names);\n\nendfunction\n\n\n%!test\n## 没有账本 ⇒ 空 cell（老行为）\n%! assert (isempty (__webassets_available__ (\"/tmp/definitely-not-here-9d3f.json\")));\n\n%!test\n## 有账本 ⇒ 全量、字典序\n%! p = tempname ();\n%! fid = fopen (p, \"w\");\n%! fputs (fid, '{\"available\":[\"plotbridge\",\"statistics\",\"convhulln\"],');\n%! fputs (fid, '\"loaded\":[\"plotbridge\"],\"pkg_available\":[\"statistics\"],\"pkg_loaded\":[],\"stamp\":1}');\n%! fclose (fid);\n%! assert (__webassets_available__ (p), {\"convhulln\", \"plotbridge\", \"statistics\"});\n%! unlink (p);\n", "/usr/src/octave/m/pkg/__webassets_info__.m": "## __webassets_info__ — 读页面加载器落的\"资产账本\"（小口子 4）\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## ── 为什么需要它 ────────────────────────────────────────────────────────────\n## 本构建的能力有一半在 `assets/` 里**按需装载**（Forge 包、`.oct`、`.m` 覆写…）。但 Octave\n## 的解释器**看不见 JS 的加载器对象**，于是\"有哪些东西可加载、哪些已经装了\"这件事在 `.m` 侧\n## 完全不可问 —— 后果是 `pkg load statistics` 只回一句\n## **`package statistics is not installed`**（实测），而 statistics 明明就在 `assets/pkg/`\n## 里等着被装。这句话会让人以为\"这个构建没有 statistics\"。\n##\n## 桥的做法：加载器在 **init 之后**与**每次装载成功之后**把账本写进 MEMFS\n##（`bridge/assets-loader.js` 的 `publish()`）：\n##     /tmp/webassets.json = { available:[…], loaded:[…], pkg_available:[…], pkg_loaded:[…], stamp }\n## 本文件只是**读它**（`jsondecode` 本构建可用，见批次 1a）。\n##\n## ── 三种名字 ────────────────────────────────────────────────────────────────\n##   · `available`     — 清单里的全部资产名（含 plotbridge/doc-cache 这类**基础设施**）\n##   · `loaded`        — 已装载的\n##   · `pkg_available` — 其中**是 Forge 包**的那些（`assets/pkg/*.js`）⇒ 给 `pkg list` 用\n## 账本不存在（例如 `.m` 侧被单独调用、或页面还没 init）时**返回空结构**，不报错 ——\n## 调用方（`pkg.m`）要能\"没有账本就照旧干活\"。\n##\n## 判据写在纯函数里是为了**宿主可测**（写一个假账本文件即可，不需要页面）。\n\nfunction info = __webassets_info__ (path)\n\n  if (nargin < 1 || isempty (path))\n    path = \"/tmp/webassets.json\";\n  endif\n\n  info = struct (\"available\", {{}}, \"loaded\", {{}}, ...\n                 \"pkg_available\", {{}}, \"pkg_loaded\", {{}}, \"stamp\", 0, \"ok\", false);\n\n  if (! (ischar (path) && exist (path, \"file\")))\n    return;                     ## 没有账本 ⇒ 空结构 + ok=false（调用方自己决定怎么办）\n  endif\n\n  try\n    raw = jsondecode (fileread (path));\n  catch\n    return;                     ## 坏账本同上：不抛错，避免把 pkg 整个弄坏\n  end_try_catch\n\n  fn = {\"available\", \"loaded\", \"pkg_available\", \"pkg_loaded\"};\n  for k = 1:numel (fn)\n    if (isfield (raw, fn{k}))\n      v = raw.(fn{k});\n      if (ischar (v))\n        v = {v};                ## 只有一条时 jsondecode 会给 char，不是 cell\n      endif\n      if (iscellstr (v))\n        info.(fn{k}) = v(:).';\n      endif\n    endif\n  endfor\n  if (isfield (raw, \"stamp\") && isnumeric (raw.stamp))\n    info.stamp = raw.stamp;\n  endif\n  info.ok = true;\n\nendfunction\n\n\n%!test\n## 没有账本（或路径不存在）⇒ 空结构 + ok=false，**不报错**\n%! i = __webassets_info__ (\"/tmp/definitely-not-here-9d3f.json\");\n%! assert (i.ok, false);\n%! assert (isempty (i.available));\n%! assert (isempty (i.pkg_available));\n\n%!test\n## 正常账本：四条名单都读进来（含\"只有一条 ⇒ char 不是 cell\"的那条路径）\n%! p = tempname ();\n%! fid = fopen (p, \"w\");\n%! fputs (fid, '{\"available\":[\"plotbridge\",\"statistics\",\"optim\"],\"loaded\":[\"plotbridge\"],');\n%! fputs (fid, '\"pkg_available\":[\"statistics\",\"optim\"],\"pkg_loaded\":[],\"stamp\":123}');\n%! fclose (fid);\n%! i = __webassets_info__ (p);\n%! assert (i.ok, true);\n%! assert (numel (i.available), 3);\n%! assert (i.loaded, {\"plotbridge\"});\n%! assert (i.pkg_available, {\"statistics\", \"optim\"});\n%! assert (isempty (i.pkg_loaded));\n%! assert (i.stamp, 123);\n%! unlink (p);\n\n%!test\n## 坏账本（不是合法 JSON）⇒ 同样不报错，只是 ok=false\n%! p = tempname ();\n%! fid = fopen (p, \"w\"); fputs (fid, \"{not json at all\"); fclose (fid);\n%! i = __webassets_info__ (p);\n%! assert (i.ok, false);\n%! unlink (p);\n", "/usr/src/octave/m/pkg/__webassets_pending__.m": "## __webassets_pending__ — **可加载但还没装载**的 Forge 包（小口子 4）\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## 用途：`pkg list` 末尾那段\"本构建还能装哪些包\"、以及 `pkg load` 的提示都问它。\n## 与 `__webassets_available__` 的分工：\n##   · `__webassets_available__()` —— 清单里的**全部**资产（含 plotbridge/doc-cache 这类基础设施）\n##   · `__webassets_pending__()`   —— 其中**是包**且**尚未装载**的（用户真正关心的那句答案）\n## 账本缺失时返回空 cell（`pkg.m` 会因此退回\"什么都不说\"的老行为，不报错）。\n\nfunction names = __webassets_pending__ (path)\n\n  if (nargin < 1 || isempty (path))\n    path = \"/tmp/webassets.json\";\n  endif\n\n  info = __webassets_info__ (path);\n  names = {};\n\n  if (! info.ok)\n    return;\n  endif\n\n  for k = 1:numel (info.pkg_available)\n    nm = info.pkg_available{k};\n    if (! any (strcmp (nm, info.pkg_loaded)))\n      names{end + 1} = nm;\n    endif\n  endfor\n\n  names = sort (names);\n\nendfunction\n\n\n%!test\n## 没有账本 ⇒ 空（老行为），不报错\n%! assert (isempty (__webassets_pending__ (\"/tmp/definitely-not-here-9d3f.json\")));\n\n%!test\n## 可用减已装：装载过的从待装名单里消失，顺序按字典序排\n%! p = tempname ();\n%! fid = fopen (p, \"w\");\n%! fputs (fid, '{\"available\":[\"plotbridge\",\"statistics\",\"optim\",\"signal\"],');\n%! fputs (fid, '\"loaded\":[\"plotbridge\",\"optim\"],\"pkg_available\":[\"optim\",\"signal\",\"statistics\"],');\n%! fputs (fid, '\"pkg_loaded\":[\"optim\"],\"stamp\":7}');\n%! fclose (fid);\n%! assert (__webassets_pending__ (p), {\"signal\", \"statistics\"});\n%! unlink (p);\n"}
};

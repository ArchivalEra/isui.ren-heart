// 生成物，勿手改：由 build/assets.py bundle-m 产出
window.__OCT_ASSETS__ = window.__OCT_ASSETS__ || {};
window.__OCT_ASSETS__["webaudiorec"] = {
  addpath: ["/usr/src/octave/m/webaudiorec"],
  files: {"/usr/src/octave/m/webaudiorec/__pra_enqueue__.m": "## Append a recording action for the page to drain (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## One line per action, same shape as the playback queue:\n##   <id>\\t<action>[\\t<args…>]\n## The page (bridge/webaudiorec.js) consumes the file, then writes progress and\n## samples back to /tmp/pra_<id>.txt and /tmp/pra_<id>.f64.\n\nfunction __pra_enqueue__ (id, action, varargin)\n\n  fid = fopen (\"/tmp/pra_queue.txt\", \"a\");\n  if (fid < 0)\n    error (\"audiorecorder: cannot write /tmp/pra_queue.txt\");\n  endif\n  fprintf (fid, \"%d\\t%s\", id, action);\n  for k = 1:numel (varargin)\n    fprintf (fid, \"\\t%.17g\", varargin{k});\n  endfor\n  fprintf (fid, \"\\n\");\n  fclose (fid);\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__pra_get__.m": "## Read one property of a recorder record (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Unknown names return [] rather than erroring: @audiorecorder's\n## __get_properties__ asks for the whole set at once, and a hard failure there\n## would break `disp (recorder)`.\n\nfunction v = __pra_get__ (id, field)\n\n  __pra_init__ ();\n  global __pra__;\n\n  v = [];\n  if (id < 1 || id > numel (__pra__.items)), return; endif\n  rec = __pra__.items{id};\n  if (isstruct (rec) && isfield (rec, field))\n    v = rec.(field);\n  endif\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__pra_id__.m": "## Pull the integer id out of an audiorecorder handle (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## @audiorecorder passes `struct (recorder).recorder` to every __recorder_*\n## builtin, so the handle is a struct with an Id field.  Accepting a bare id and\n## the classdef object too keeps these .m files callable directly (which the\n## acceptance suite does) — same forgiving contract as __pba_id__.\n\nfunction id = __pra_id__ (handle)\n\n  id = 0;\n\n  if (isnumeric (handle) && isscalar (handle))\n    id = handle;\n    return;\n  endif\n\n  if (isstruct (handle))\n    if (isfield (handle, \"Id\"))\n      id = handle.Id;\n    elseif (isfield (handle, \"recorder\") && isstruct (handle.recorder) ...\n            && isfield (handle.recorder, \"Id\"))\n      id = handle.recorder.Id;\n    endif\n    return;\n  endif\n\n  try\n    r = struct (handle).recorder;\n    if (isstruct (r) && isfield (r, \"Id\"))\n      id = r.Id;\n    endif\n  catch\n    id = 0;\n  end_try_catch\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__pra_init__.m": "## WebAudio **录音** 侧的 Octave 底座（T7 / 缺口清单 B1）\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## ── 架构（与播放侧的 build/webaudio/ 刻意对称）────────────────────────────────\n## `@audiorecorder` 那 19 个方法调的是 19 个 `__recorder_*` builtin，本构建里\n## 它们一个都不存在（`audiodevinfo.cc` 里那批全被 `#if defined (HAVE_PORTAUDIO)`\n## 编掉了），所以 `audiorecorder(8000,8,1)` 直接报 `'__recorder_audiorecorder__'\n## undefined`。这里用**纯 .m** 把它们补齐，零编译。\n##\n## 句柄：`@audiorecorder` 的构造是\n##     recorder.recorder = __recorder_audiorecorder__ (...)\n## 之后每个方法拿 `struct (recorder).recorder` 传回来。**传回来的是副本**，\n## 所以可变状态不能放在句柄里 —— 与播放侧同一个理由，句柄只是一个整数 id，\n## 真状态在全局表里：\n##     __pra__.items{id}   该录音机的属性（Fs/Nbits/Channels/DevID/Tag/UserData）\n##     __pra__.next        下一个空闲 id\n## 取值/写值一律走 `__pra_new__`/`__pra_get__`/`__pra_put__` —— 直接返回全局表的\n## 函数会把**副本**交出去，之后所有写入都会静默丢失（这条播放侧已经踩过）。\n##\n## ── 真正的录音在页面侧 ──────────────────────────────────────────────────────\n## Octave 无法同步调用 JS，所以这里只**记录意图**：动作追加到\n## `/tmp/pra_queue.txt`，由 `bridge/webaudiorec.js` 轮询后落实。音频数据由页面写回\n## `/tmp/pra_<id>.f64`（交织的 little-endian double），进度/错误写\n## `/tmp/pra_<id>.txt`（`key<TAB>value` 若干行，见 `__pra_progress__`）。\n##\n## ── 为什么用 MediaRecorder 而不是 ScriptProcessorNode ────────────────────────\n## 实测：wasm 里 Octave 的 `pause(1)` **不是完全阻塞**主线程，但让出的机会很稀疏\n## （1 秒里 JS 定时器只跑了约 5 次）。按主线程回调取样的 ScriptProcessorNode\n## 在这个节奏下会大面积丢样本，而 MediaRecorder 的采集/编码不走主线程，\n## 因此是这条架构下唯一可靠的选择（代价：**只有解码完成后才知道样本数**）。\n##\n## 与播放侧一致的口径：`playblocking` 那种\"真阻塞\"语义本构建做不到，\n## 录音侧 `recordblocking` 改成**轮询等待**（见 `__recorder_recordblocking__`）。\n\nfunction __pra_init__ ()\n\n  global __pra__;\n  if (isempty (__pra__))\n    __pra__ = struct ();\n    __pra__.items = {};\n    __pra__.next = 1;\n  endif\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__pra_new__.m": "## Allocate a recorder record (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction id = __pra_new__ (props)\n\n  __pra_init__ ();\n  global __pra__;\n\n  id = __pra__.next;\n  __pra__.next = id + 1;\n  __pra__.items{id} = props;\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__pra_progress__.m": "## Read the page's progress/error report for one recorder (own code).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## bridge/webaudiorec.js owns the real state machine (it is the only party that\n## can await getUserMedia) and mirrors it into /tmp/pra_<id>.txt as `key<TAB>value`\n## lines.  Recognised keys:\n##\n##   state   idle | recording | paused | decoding | done | denied | insecure | error\n##   frames  frames known so far (an estimate while recording; exact after done)\n##   err     message for denied/insecure/error\n##\n## A missing file means \"the page has not looked at our request yet\" and reports\n## state = \"idle\" — that is the honest reading, and it matters: __recorder_record__\n## returns immediately, so a caller that checks right away must not be told\n## \"stopped\" or \"done\".\n##\n## ⚠️ 这里读的是**页面写下的**东西，所以 state 永远滞后于 Octave 侧刚入队的动作。\n## 谁需要\"等到真的结束\"就用 __recorder_recordblocking__ 那种轮询，别假设即时。\n\nfunction s = __pra_progress__ (id)\n\n  s = struct (\"state\", \"idle\", \"frames\", 0, \"err\", \"\");\n\n  fn = sprintf (\"/tmp/pra_%d.txt\", id);\n  if (! exist (fn, \"file\"))\n    return;\n  endif\n\n  try\n    lines = strsplit (fileread (fn), \"\\n\");\n  catch\n    return;\n  end_try_catch\n\n  for k = 1:numel (lines)\n    line = lines{k};\n    if (isempty (line)), continue; endif\n    tab = find (line == \"\\t\", 1);\n    if (isempty (tab)), continue; endif\n    key = line(1:tab-1);\n    val = line(tab+1:end);\n    switch (key)\n      case \"state\"\n        s.state = val;\n      case \"frames\"\n        s.frames = str2double (val);\n      case \"err\"\n        s.err = val;\n    endswitch\n  endfor\n\n  if (isnan (s.frames))\n    s.frames = 0;\n  endif\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__pra_put__.m": "## Write one property of a recorder record (own code, repo license).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction __pra_put__ (id, field, value)\n\n  __pra_init__ ();\n  global __pra__;\n\n  if (id < 1 || id > numel (__pra__.items)), return; endif\n  rec = __pra__.items{id};\n  if (! isstruct (rec)), rec = struct (); endif\n  rec.(field) = value;\n  __pra__.items{id} = rec;\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_audiorecorder__.m": "## Construct a recorder record (T7). Official signature __recorder_audiorecorder__.\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Mirrors the builtin's contract: 0–4 args, defaults 8000 Hz / 8 bits / 1 channel\n## (MATLAB's defaults for `audiorecorder`).  DEVID is recorded but not used — the\n## browser exposes exactly one device, see `audiodevinfo`'s static model.\n##\n## Returns the opaque handle: a struct with an Id, matching what @audiorecorder\n## stores in its `.recorder` field and hands back to every other builtin.\n\nfunction h = __recorder_audiorecorder__ (varargin)\n\n  n = numel (varargin);\n  if (n > 4)\n    print_usage ();\n  endif\n\n  fs = 8000; nbits = 8; nchans = 1; devid = -1;\n  if (n >= 1), fs     = varargin{1}; endif\n  if (n >= 2), nbits  = varargin{2}; endif\n  if (n >= 3), nchans = varargin{3}; endif\n  if (n >= 4), devid  = varargin{4}; endif\n\n  ## 官方那句 FIXME 说\"内部 C++ 函数不做输入校验\"，这里补一点：\n  ## 明显不合理的参数宁可早点报错，也别让页面去做无意义的 getUserMedia。\n  if (! (isscalar (fs) && fs > 0 && fs == fix (fs)))\n    error (\"audiorecorder: sample rate must be a positive integer\");\n  endif\n  if (! (isscalar (nbits) && nbits > 0 && nbits == fix (nbits)))\n    error (\"audiorecorder: bits per sample must be a positive integer\");\n  endif\n  if (! (isscalar (nchans) && nchans > 0 && nchans == fix (nchans)))\n    error (\"audiorecorder: channel count must be a positive integer\");\n  endif\n\n  props = struct (\"Fs\", fs, \"Nbits\", nbits, \"Channels\", nchans, ...\n                  \"DevID\", devid, \"Tag\", \"\", \"UserData\", [], ...\n                  \"Recording\", false);\n\n  id = __pra_new__ (props);\n\n  h = struct (\"Id\", id);\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_get_channels__.m": "## Channel count of a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction v = __recorder_get_channels__ (h)\n  v = __pra_get__ (__pra_id__ (h), \"Channels\");\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_get_fs__.m": "## Sample rate of a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction v = __recorder_get_fs__ (h)\n  v = __pra_get__ (__pra_id__ (h), \"Fs\");\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_get_id__.m": "## Device id of a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Recorded as given (default -1) but not used: the browser exposes exactly one\n## capture device, see audiodevinfo's static model.\n\nfunction v = __recorder_get_id__ (h)\n  v = __pra_get__ (__pra_id__ (h), \"DevID\");\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_get_nbits__.m": "## Bits per sample of a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction v = __recorder_get_nbits__ (h)\n  v = __pra_get__ (__pra_id__ (h), \"Nbits\");\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_get_sample_number__.m": "## Frames captured so far (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## While recording this is the page's **estimate** (elapsed × rate), because\n## MediaRecorder only yields the real sample count after it is stopped and the\n## blob decoded — see the note in __pra_init__.  After \"done\" it is exact.\n\nfunction v = __recorder_get_sample_number__ (h)\n  s = __pra_progress__ (__pra_id__ (h));\n  v = s.frames;\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_get_tag__.m": "## User tag of a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction v = __recorder_get_tag__ (h)\n  v = __pra_get__ (__pra_id__ (h), \"Tag\");\n  if (isempty (v)), v = \"\"; endif\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_get_total_samples__.m": "## Total frames in the recording (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction v = __recorder_get_total_samples__ (h)\n  s = __pra_progress__ (__pra_id__ (h));\n  v = s.frames;\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_get_userdata__.m": "## User data attached to a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction v = __recorder_get_userdata__ (h)\n  v = __pra_get__ (__pra_id__ (h), \"UserData\");\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_getaudiodata__.m": "## Return the recorded audio (T7).  **朝向是 声道 × 帧**，不是帧 × 声道。\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## ⚠️ 朝向这条是**读 @audiorecorder/getaudiodata.m 的收尾**定出来的，不是猜的：\n## 那个 .m 最后做的是\n##     if (get (recorder, \"NumberOfChannels\") == 2)\n##       data = data.';        # 立体声：原样转置\n##     else\n##       data = data(1,:).';   # 单声道：**取第 1 行**\n##     endif\n## 所以本函数必须交回 声道×帧；单声道还必须**至少有 1 行**，否则那句 `data(1,:)`\n## 直接 \"out of bound 0\"（第一版就是栽在这里：返回了 0×1，单声道路径一读就报错）。\n## 于是空数据也要返回 **nch×0**（单声道 = 1×0，转置后 0×1，与桌面版\"还没录到东西\"\n## 的形状一致）。\n##\n## 数据来自页面写下的 /tmp/pra_<id>.f64（交织的 little-endian double，与播放侧同布局）。\n## Octave 的裸读是列优先，所以 reshape 成 (声道 × 帧) 正好 —— 不用再转置。\n##\n## ── 权限三态必须**分开报**（外部审核 B1 明确要求）──────────────────────────\n## \"允许 → 正常录音 / 拒绝 → Octave 可读的明确错误 / 没有安全上下文 → 明确错误\"。\n## 尤其**不能假装麦克风永远存在**：拿不到数据时给一句能照着做的错误，\n## 而不是返回空矩阵让调用者去猜。\n\nfunction data = __recorder_getaudiodata__ (h)\n\n  id = __pra_id__ (h);\n  if (id < 1)\n    error (\"audiorecorder: invalid recorder handle\");\n  endif\n\n  nch = __pra_get__ (id, \"Channels\");\n  if (isempty (nch) || ! (isscalar (nch) && nch >= 1))\n    nch = 1;\n  endif\n\n  s = __pra_progress__ (id);\n\n  ## 三态分开报；页面的原文（若有）作为括号里的补充，不自造细节。\n  if (strcmp (s.state, \"denied\"))\n    msg = [\"audiorecorder: microphone access was denied by the user or by the \" ...\n           \"browser. Grant microphone permission for this origin, then record again.\"];\n    if (! isempty (s.err))\n      msg = [msg \" (\" s.err \")\"];\n    endif\n    error (\"%s\", msg);\n  elseif (strcmp (s.state, \"insecure\"))\n    msg = [\"audiorecorder: microphone capture requires a secure context \" ...\n           \"(https://, or http://localhost / http://127.0.0.1).\"];\n    if (! isempty (s.err))\n      msg = [msg \" (\" s.err \")\"];\n    endif\n    error (\"%s\", msg);\n  elseif (strcmp (s.state, \"error\"))\n    error (\"audiorecorder: recording failed: %s\", s.err);\n  endif\n\n  ## 空数据也要是 nch×0（见文件头：单声道路径会 data(1,:)）\n  data = zeros (nch, 0);\n\n  fn = sprintf (\"/tmp/pra_%d.f64\", id);\n  if (! exist (fn, \"file\"))\n    return;\n  endif\n\n  fid = fopen (fn, \"rb\");\n  if (fid < 0)\n    return;\n  endif\n  raw = fread (fid, Inf, \"double\");\n  fclose (fid);\n\n  n = floor (numel (raw) / nch);\n  if (n < 1)\n    return;\n  endif\n\n  raw = raw(1:n*nch);\n  data = reshape (raw, nch, n);\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_isrecording__.m": "## Is the recorder currently recording? (T7)\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## The page is authoritative, but it is also **asynchronous**: right after\n## `record (r)` the page may not have drained the queue yet, so its status file\n## still says \"idle\".  Reporting false there would contradict what the user just\n## asked for, so \"idle\" falls back to the flag __recorder_record__ set.\n## Anything else (done/denied/insecure/error/paused) is honestly not recording.\n\nfunction tf = __recorder_isrecording__ (h)\n\n  id = __pra_id__ (h);\n  if (id < 1)\n    error (\"audiorecorder: invalid recorder handle\");\n  endif\n\n  s = __pra_progress__ (id);\n\n  if (strcmp (s.state, \"idle\"))\n    tf = logical (__pra_get__ (id, \"Recording\"));\n  else\n    tf = strcmp (s.state, \"recording\");\n  endif\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_pause__.m": "## Pause an ongoing recording (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## MediaRecorder has a real pause(), so this is not a no-op — the page calls it.\n\nfunction __recorder_pause__ (h)\n\n  id = __pra_id__ (h);\n  if (id < 1)\n    error (\"audiorecorder: invalid recorder handle\");\n  endif\n  __pra_enqueue__ (id, \"pause\");\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_record__.m": "## Start recording without blocking (T7).\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## `record (r)` records until stopped; `record (r, len)` records for len seconds.\n## This is the **non-blocking** form (MATLAB semantics): it returns immediately,\n## the page does the actual capture.  Use __recorder_recordblocking__ to wait.\n##\n## len = 0 encodes \"until stop\" in the queue.\n\nfunction __recorder_record__ (h, len)\n\n  id = __pra_id__ (h);\n  if (id < 1)\n    error (\"audiorecorder: invalid recorder handle\");\n  endif\n\n  if (nargin < 2 || isempty (len))\n    len = 0;\n  elseif (! (isscalar (len) && len >= 0))\n    error (\"audiorecorder: length must be a non-negative number of seconds\");\n  endif\n\n  fs = __pra_get__ (id, \"Fs\");\n  nbits = __pra_get__ (id, \"Nbits\");\n  nch = __pra_get__ (id, \"Channels\");\n\n  __pra_put__ (id, \"Recording\", true);\n  __pra_enqueue__ (id, \"record\", fs, nbits, nch, len);\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_recordblocking__.m": "## recordblocking: **本构建不支持** —— 如实报错，不静默降级。 (T7)\n## SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## ── 为什么做不到（**实测**，别推翻自己）────────────────────────────────────\n## 这条一开始被记成\"不需要 Asyncify\"，依据是一次不可靠的测量：当时只看\"定时器总共\n## 跑了多少次\"，把 eval **前后**的 tick 也算进去了，于是得出\"pause() 会让出主线程\"。\n## 精确复测（记录每个 tick 的时刻，只数落在 eval 区间内的）结果是：\n##\n##     pause(1)                          → 区间内 tick = 0（理论上限 20）\n##     pause(2)                          → 区间内 tick = 0（上限 40）\n##     for k=1:10, pause(0.1), endfor    → 区间内 tick = 0（上限 20）\n##\n## ⇒ **`pause()` 期间浏览器事件循环完全停摆**（阻塞式睡眠）。\n## 而 recordblocking 的语义就是\"等页面把录音做完\"——等待期间页面必须能跑，\n## 这两件事直接冲突。真要做得给 wasm 加 Asyncify（HANDOFF 的 T10）。\n##\n## 反过来这也解释了两件已验证的事：\n##   · `input()` 能用，是因为 `window.prompt` 是**同步**的浏览器 API；\n##   · `uigetfile`（要等一个**异步**的文件选择框）与 recordblocking 同病。\n##\n## 报错里直接把替代用法写清楚，省得用户去猜。\n\nfunction __recorder_recordblocking__ (h, len)\n\n  error ([\"audiorecorder: recordblocking is not available in this build. \" ...\n          \"It must wait for the page to finish recording, but Octave cannot \" ...\n          \"yield to the browser while it waits (measured: the JS event loop \" ...\n          \"gets 0 turns during pause()), so this needs Asyncify -- see T10. \" ...\n          \"Use record (r, len) and read the samples with getaudiodata (r) \" ...\n          \"once the recording has finished.\"]);\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_resume__.m": "## Resume a paused recording (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction __recorder_resume__ (h)\n\n  id = __pra_id__ (h);\n  if (id < 1)\n    error (\"audiorecorder: invalid recorder handle\");\n  endif\n  __pra_enqueue__ (id, \"resume\");\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_set_fs__.m": "## Change the sample rate of a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n##\n## Written straight into the record: the page reads Fs from the `record` queue\n## line, so a rate set before `record` takes effect (a rate set mid-recording\n## does not — same as desktop, where the stream is already open).\n\nfunction __recorder_set_fs__ (h, v)\n\n  if (! (isscalar (v) && v > 0 && v == fix (v)))\n    error (\"audiorecorder: sample rate must be a positive integer\");\n  endif\n  __pra_put__ (__pra_id__ (h), \"Fs\", v);\n\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_set_tag__.m": "## Set the user tag of a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction __recorder_set_tag__ (h, v)\n  __pra_put__ (__pra_id__ (h), \"Tag\", v);\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_set_userdata__.m": "## Attach user data to a recorder (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction __recorder_set_userdata__ (h, v)\n  __pra_put__ (__pra_id__ (h), \"UserData\", v);\nendfunction\n", "/usr/src/octave/m/webaudiorec/__recorder_stop__.m": "## Stop recording (T7). SPDX-License-Identifier: AGPL-3.0-or-later\n\nfunction __recorder_stop__ (h)\n\n  id = __pra_id__ (h);\n  if (id < 1)\n    error (\"audiorecorder: invalid recorder handle\");\n  endif\n\n  __pra_put__ (id, \"Recording\", false);\n  __pra_enqueue__ (id, \"stop\");\n\nendfunction\n"}
};
